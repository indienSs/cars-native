import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  MapIcon: {
    position: "absolute",
    bottom: 20,
    right: "45%",
    zIndex: 999,
  },
  image: {
    width: 35,
    height: 35
  }
});
