import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  CartItem: {
    height: 80,
    backgroundColor: "coral",
  },
  title: {
    color: "white",
    fontSize: 24,
    paddingTop: 40,
    paddingLeft: 20,
  },
});
